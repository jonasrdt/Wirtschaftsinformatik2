

#  zaehler = Zähler
#  range(start, stop, step)
# This code is using a `for` loop to iterate over a range of numbers from 1 to 11 (excluding 11) with
# a step of 2. During each iteration, the current value of the loop variable `zaehler` is printed
# along with a message. The output will be:
for zaehler in range(1,11, 2):
    print("Der Zähler steht aktuell bei:", zaehler)
